## 部署
#### 修改文章或配置内容后，首先更新到远程仓库，以便多端同步

```shell
git add .
git commit -m "描述你修改的内容"
git push origin main
```

#### 在修改文章之前，也需要拉取最新配置

```shell
git pull origin main
```

保持在最新main分支状态后，执行部署

```shell
GIT_USER=M0dZer0 USE_SSH=true npm run deploy
```

## 创作

只有二级和三级标题才会出现在右边目录，四级标题可以用来表示步骤，五级标题不建议出现